{
  "name": "skillforge",
  "version": "1.0.0",
  "private": true,
  "description": "PM SkillForge — AI-Powered Product Manager Skills Assessment Platform",
  "scripts": {
    "server": "cd server && npm run dev",
    "app": "cd app && npx expo start",
    "install:all": "npm install --prefix server && npm install --prefix app",
    "server:install": "npm install --prefix server",
    "app:install": "npm install --prefix app"
  }
}
